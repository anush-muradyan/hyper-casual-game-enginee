using System;
using UnityEngine;

namespace Entity
{
    public class Obstacles : MonoBehaviour
    {
        [SerializeField] private float speed = 4f;

        private void Start()
        {
            //TODO:  Remove if dont need it, only example for pull request
        }
    }
}